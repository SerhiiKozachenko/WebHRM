@using Hrm.Web.Models.Menu
@model ManagerTopMenuItems

@{
    var profile = Model == ManagerTopMenuItems.MyProfile ? "active" : string.Empty;
}

<div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container">
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </a>
            
            <a class="brand" href="@Url.Action("Index", "Home")">Hrm</a>
            
            <ul class="nav pull-right">
                    <li class="dropdown @profile">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Management<b class="caret"></b></a>
                        <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
                            <li>@Html.ActionLink("Edit Profile", "Index", "Profile")</li>
                            <li>@Html.ActionLink("Log out", "LogOff", "Account")</li>
                        </ul>
                    </li>
                </ul>
        </div>
    </div>
</div>